# Auditoría integral — datos, métricas, tiempo real y código (2026-09-05)

Petición de Edgar: buscar bugs, incoherencias, código obsoleto/duplicado y comprobar que **todo** lo que
muestra el bot es correcto y en tiempo real (empezando por el retraso visible en las operaciones abiertas).
Todo lo de abajo está **medido** en el CT (192.168.1.204:9420) o cruzado contra la API de Strike, no leído.

## Método
- Muestreo del DOM de Trade cada 500 ms durante 40 s + captura de cada frame del socket (`freshness.py`).
- Volcado de los 19 endpoints REST y comprobación de identidades contables entre ellos.
- Cabecera de mercado cruzada con `premiumIndex` y `ticker/24hr` de Strike en el mismo instante.
- Capturas de todas las rutas (1440 px) con escaneo de `NaN/undefined/Infinity/9999` y errores de consola.
- Lectura de las cadencias de cada bucle del bridge y de cada poll del frontend.

## Lo que estaba bien (verificado)
- Cabecera de mercado = Strike al segundo: mark 79,685.99 / index 79,685.79 / funding 0.0000162 / 24h H-L-Vol-Change
  idénticos; countdown 927 s vs `nextFundingTime` 928 s.
- Identidades contables: Σ upnl posiciones = account.unrealized; Σ notional = position_value; equity =
  initial + realized + unrealized en performance, account, status y risk.account; available = equity − margin.
- Por posición: upnl, ROE, notional recalculados a mano coinciden en las 6.
- Ninguna ruta muestra NaN/undefined/sentinelas; 0 errores de consola; sin desbordamiento horizontal.
- Filas FUNDING: filtradas del chart y etiquetadas "carry" en Order History.

## Bugs encontrados y arreglados (con test)

### R1 · Posiciones abiertas con retraso — LA queja
**Medido:** en 40 s el mark de la posición BTC cambió **0–1 veces** mientras la cabecera (Mark Price, REST 4 s)
cambió 6 veces; los frames `positions` llegaban cada 1,2 s pero con el mismo mark. Causa: el libro trend
re-marca solo en `run_loop` (cada **60 s**) con `venue_marks` que a su vez se refrescaban cada 15 s, y el
feed de `premiumIndex` (cada 5 s) solo emitía los 4 símbolos streameados (XAU/XAG/WTI/SP500 nunca).
**Fix:** `strike_ws._mark_loop` emite los 31 mercados → `main.on_mark_price` → `TrendDailyEngine.set_venue_mark`
marca la posición al instante. El bridge emite `positions` **solo cuando cambian** (antes: 22 frames
idénticos/40 s/mercado) y **retiene** el último para clientes nuevos.

### R2 · Riesgo ciego a pérdidas abiertas (el hallazgo más grave)
El backtester que validó los tres perfiles alimenta al `RiskManager` con equity **mark-to-market** en cada barra;
el motor en vivo/paper solo le pasaba fills. Consecuencia: freno por drawdown (36 %), circuit breaker (80 % del
budget), tope de exposición y sizing veían equity realizada — un libro un 20 % bajo el agua no disparaba nada
hasta cerrar. **Fix:** `update_unrealized` + `_risk_mark_loop` (5 s); todos los límites, el pico y el drawdown
leen realizado + abierto; un fill entrega el abierto restante para no contar dos veces. La persistencia sigue
siendo el ledger realizado. Se ve en pantalla: Account/Risk "Peak" y "Drawdown" ahora son MTM y coinciden con
Portfolio (antes: 1,009.64 vs 1,010.30 en la misma pantalla).

### R3 · Supervisor de tareas desalineado
`task_names` se quedó en "metrics" cuando `run()` ganó el bucle de funding: un crash del funding se registraba
como "trend_daily" y **reiniciaba el loop del trend (una segunda copia)**; un crash del trend era "ws_user", no
reiniciable → el libro dejaba de operar en silencio. Corregido y con aviso si vuelve a desalinearse.

### R4 · Tracking modelo vs paper roto
6 filas para 3 días, todos los `paper_ret` = 0.0 y un "tracking error 7,4 %" construido sobre ellos.
`equity_basis` se sobrescribía antes de calcular el retorno (hoy vs hoy) y cada run añadía fila. Ahora una fila
por día con la base previa; al cargar el estado se deduplica por fecha.

### R5 · `/api/trades?limit` compartido con el funding
Funding liquida cada hora en cada mercado (6 posiciones = 144 filas/día): el límite de 400 pronto contendría
solo carry. Ahora el límite cuenta fills y el carry se devuelve dentro de la ventana de esos fills.

### R6 · 12 peticiones a `/api/markets` en 16 s
Once componentes con su propio timer. Un solo poll compartido (`useSyncExternalStore`), arranca con el primer
suscriptor y para con el último.

### R7 · "Strike feed x s" solo contaba trades
En un mercado fino subía a minutos con la marca moviéndose cada 5 s. Ahora cuenta trade o marca.

## Incoherencias menores corregidas
- Risk page: "Peak realised" → "Peak equity (mark-to-market)".
- Línea duplicada `_trend_symbols_sent.update(by_symbol)`.

## Observado, no tocado (decisión de Edgar)
- Bot column dice "2x" (leverage configurado del símbolo) mientras las posiciones trend van a 1x
  (margin = notional). Correcto en paper, pero lee raro al lado.
- Daily/Weekly PnL son **realizados** (como en el backtester); el límite diario no ve pérdidas abiertas del día.
- `sharpe_ratio 175` y `profit_factor 9999.99` viajan en `/api/performance` con `sharpe_valid: false`; la UI
  los oculta bien (n/a), pero el payload debería llevar null.
- Un mercado solo-venue a 1d va un día por detrás (Strike publica la diaria al cierre).
- El pico MTM no se persiste: tras un reinicio arranca desde el pico realizado (conservador).

## Verificado tras desplegar (`885a4af`, 2026-09-05 01:1xZ)
- Posición BTC: mark cambia 9×/40 s (antes 0–1×), PnL 3× (resolución de céntimos); cabecera 6×.
- Frames `positions`: WTI 5, XAG 8, SP500 12 por 40 s (antes 22 idénticos cada uno).
- risk / account / performance: pico 1,011.16 en los tres; drawdown 0.075 % en los tres (±5 s de marca).
- `/api/trades?limit=400`: 14 fills + 94 filas de carry dentro de su ventana (antes el límite lo compartían).
- Tracking: 3 filas, una por día. Journal del bridge: 0 errores en 3 min.

## Segunda ronda (madrugada del 05) — "me llegan notificaciones de compra y la UI está igual"
**Qué eran:** el rebalance diario de las 00:05 UTC (02:05 local) escalando el libro al perfil agresivo:
6 fills ENTRY (BTC 0,0011 → 0,0044, nocional total 414 → 1.375 $, 136 % del equity). La UI sí lo reflejaba
en los tamaños y en Order History; **no** en Activity ni como toast, y los mensajes decían solo "Compra".

### Arreglado (con test)
- **Los fills del trend esquivaban el hook del bridge**: `on_fill=self._process_paper_fill` se ligaba al
  construir el motor, antes de que el bridge envolviera ese método → sin fila en Activity, sin frame
  `trade` en el socket, sin toast. Ahora se resuelve en cada llamada.
- **Telegram dice qué es cada fill**: "Rebalance diario · añade a la posición / abre / reduce / cierra",
  motivo, y la posición resultante con su peso del equity; "TREND_DAILY" ya tiene nombre.
- **Fills TradFi al precio de Yahoo**: el paper llenaba oro al open de la sesión CME (4.477) cuando Strike
  estaba a 4.435 → la posición nacía un 1 % bajo el agua. Ahora se llena a la **marca del venue** en el
  momento de ejecutar; la señal sigue leyendo las velas diarias.
- **El funding contaba como "pérdidas consecutivas"**: 4 liquidaciones horarias negativas armaban la pausa
  ("Consecutive loss pause", Activity 17:00Z) y entraban en el modelo de ruina. Ahora es un cash flow.
- Las cuatro recomendaciones aceptadas: límites diario/semanal **mark-to-market** (Account muestra el PnL
  del día con las posiciones abiertas, y lo realizado aparte); cabecera del bot muestra el leverage de la
  posición abierta (`1x`) o `≤2x` si no hay; `/api/performance` envía `null` en vez de Sharpe 175 / PF
  9999.99; el pico MTM se persiste en `data/risk_peak.json` y se restaura al reiniciar.

### Tercera ronda — la comisión de entrada al fill (hecho)
Una sola regla de caja para todas las capas (`trade_database.models.cash_effect` / `fee_paid`):
ENTRY → −fee al fill · EXIT → pnl (neto round-trip, lo que leen las estadísticas) + `entry_fee_charged`
(la parte de entrada que ya se pagó) · FUNDING → pnl. Columna nueva con migración de esquema (v3),
ledger vivo, cadena de restauración, all-time (curva de equity con todas las filas, max DD sobre la
misma curva, fees cobradas una vez), portfolio (realizado, fees, tabla diaria), `fees_today`, filas de
`/api/trades` (`entry_fee_charged`, `cash_effect`), simulador intradía y libro trend. Las posiciones
abiertas antes del cambio no pagaron nada al entrar: su cierre cobra la entrada como siempre y no
acredita nada (`entry_fee_paid` por posición, prorrata en cierres parciales). Las filas antiguas de la DB
encadenan exactamente igual que antes. El serializador ya no lee "fee > 0" como salida.

## Cuarta ronda — auditoría de la estrategia (modelo, entradas, salidas, riesgo, datos)
Método: lectura del modelo y del motor, reproducción **independiente** de los pesos de hoy con datos
frescos, comparación vela a vela de la caché del CT con una descarga nueva, y cruce de reglas del venue.

### Verificado correcto
- Modelo (`trend_daily_model.py`): Donchian ensemble 5/10/20/30/60/90 con stop nunca-descendente en el
  mid, escalado por vol (90 d, 0,8 objetivo, tope 3x), 1/N, umbral de rebalance 20 %, universo mensual
  point-in-time con diversidad por clase y tope de correlación. Usa solo datos ≤ fecha de decisión.
- Reproducción de los pesos de hoy (datos frescos): BTC 0,3497 vs 0,3510 · SOL 0,1602 vs 0,1608 ·
  WTI 0,1378 vs 0,1377 · SP500 0,4167 vs 0,4167 · XAU 0,2280 vs 0,2247 (diferencias < 0,4 %, explicadas
  por UNA vela distinta por mercado en la caché del CT — ver abajo).
- Fills del 05 respetan step/min-notional del venue (BTC 1e-5, SOL 0,01, SP500 0,001, WTI 0,01, XAG
  0,001, XAU 0,001); Strike lista los TradFi como PERPETUAL/trading sin horario → operan 24/7.
- Contabilidad sincronizada: Σ `cash_effect` de la DB (9,6077) = ledger realizado (9,6077) al céntimo.
- Salidas: la escalera de stops se evalúa en la corrida diaria sobre cierres (como el backtest); sin
  SL/TP intradía "by design" ✓. Kill del edge monitor → `close_all`; halt por drawdown 36 % → flatten ✓.
- Fin de semana: Yahoo no tiene barras; la decisión usa el último cierre completo (viernes) ✓.

### Bugs encontrados y arreglados (con test)
- **S1 · Velas Yahoo contaminadas en la caché (grave).** Entre 18:00 y 00:00 ET Yahoo muestra la sesión
  Globex NUEVA bajo la fecha del día anterior. La corrida de las 00:05 UTC leía esa hora de sesión como
  cierre del día y la cacheaba como completa: plata "3-sep" = o 67,63 h 67,69 l 67,51 c 67,59 (vol 526)
  cuando el cierre real fue 66,97. Con la vela real, XAG pesa 0,040; el CT calculó 0,079 (2 lookbacks
  largos en vez de 1) → **la posición de plata está al doble**. Igual en XAU (4526 vs 4491,7) y WTI
  (91,79 vs 91,30) el 3-sep. Arreglo: las velas Yahoo se fechan por el calendario de la bolsa, la del
  día en curso nunca se devuelve, y la caché relee los últimos 5 días en cada refresco (la vela
  asentada gana). **La ejecución pasa de 00:05 a 04:05 UTC** (medianoche ET): cuesta una fracción de
  deriva diaria en 4 mercados cripto; la alternativa (señal TradFi con un día de retraso en los otros
  4) cuesta mucho más. El rebalance de mañana corrige XAG solo (cambio > 20 %).
- **S2 · Los frenos de riesgo no tocaban la única estrategia viva.** Límite diario/semanal, circuit
  breaker y halt por drawdown solo pasaban por `check_signal` (estrategias retiradas). Ahora el libro
  trend pregunta `can_add_exposure()` en cada corrida: sin riesgo nuevo mientras un límite esté en
  vigor (ni siquiera el re-alineado por equity), salidas y reducciones siempre ejecutan; se registra
  en Activity y en `/api/trend.last_adds_blocked`.

### Observado, documentado, no cambiado (decisión de diseño validada)
- `ANNUALIZATION = 365` para todos: las series TradFi tienen ~252 barras/año → su vol anualizada sale
  1,2× y sus posiciones ~17 % más pequeñas respecto al objetivo. Es la convención con la que se validó;
  cambiarla exige revalidar.
- El re-alineado diario al peso (delta ≥ 10 $) reproduce el modelo de retorno (pesos constantes) y su
  coste no está en el backtest: ~0,7 %/año a 10 k$, menor a 1 k$.
- La caché local de este PC tenía velas cripto del 31-ago a medias (artefacto de investigación, no
  del CT, y no está en git).

## Quinta ronda — las dos mejoras de experto, revalidadas, y la UI en Chrome real
- **Anualización por activo** (cripto 365 días, TradFi 252): las series de futuros/índices tienen ~252
  barras/año; con 365 su vol salía 1,2× y sus posiciones ~17 % por debajo del objetivo. Cambiado en el
  modelo y en la investigación y **revalidado en los tres perfiles: 11/11 en los tres**.
  | perfil | Sharpe | CAGR | maxDD | peor día | peor semana | bajo pico |
  |---|---|---|---|---|---|---|
  | conservador 0,10 | 1,93 | 6,3 % | 4,2 % | −1,45 % | −2,00 % | 599 d |
  | balanced 0,45 | 1,89 | 27,9 % | 17,6 % | −6,13 % | −8,50 % | 613 d |
  | agresivo 0,80 | 1,84 | 43,9 % | 29,8 % | −9,08 % | −12,38 % | 721 d |
  Las escaleras de pérdida se recortan de la cola con el mismo método (×1,25 día, ×1,20 semana, ×1,30
  drawdown): conservador 2/3/6 % (el 1 % diario anterior estaba POR DEBAJO del peor día visto),
  balanced 8/11/23 %, agresivo 12/15/39 %. Los defaults de Settings siguen al balanced.
- **Banda muerta en el re-alineado**: un peso que el modelo mantiene solo se re-alinea si la deriva en
  dólares supera el 20 % de la posición (o el mínimo del venue); salidas y cambios de peso siempre.
- **Caché diaria precalentada al arrancar** (`warm_cache`): las escaleras de salida y MAE/MFE leen
  velas cacheadas; con la caché vacía toda fila TradFi mostraba "---" (lo que vio Edgar). Caché limpia
  empujada al CT.
- **UI**: "Liq. ---" → "none · 1x"; "Regime ---" en filas trend → "daily · n/a"; trigger legible;
  bloque "Next order (estimate)" (leverage 2x intradía, engañoso en un libro trend) → "Next rebalance
  (estimate)" con peso objetivo, nocional objetivo, tenido, deriva y si opera; texto de Balanced
  ("la que corre hoy") corregido. Revisado en Chrome real: todas las posiciones con datos completos y
  coherentes con la API.

## Sexta ronda — lo que la verificación en Chrome real sacó a la luz (a6e2ada → 5c6c34a, 472 tests)
- **"---" y "Loading strategies…" en una pestaña en segundo plano**: `usePolling` saltaba también la
  PRIMERA petición mientras la pestaña estaba oculta (`document.visibilityState === "hidden"`), así que
  una pestaña abierta detrás se quedaba vacía aunque `/api/*` contestara en 10 ms. Ahora la primera sale
  siempre; los ticks periódicos siguen esperando a la visibilidad (a6e2ada). Verificado: cabecera
  (mark/index/funding/24h/OI/spread) y columna Bot ("Next rebalance (estimate)") pobladas en una pestaña
  oculta.
- **"All-time max 0,00 %" bajo un drawdown vivo de 0,37 %**: el máximo de la cadena realizada ignoraba
  el libro abierto; `/api/performance.max_drawdown` ahora es como mínimo el drawdown mark-to-market
  (3587b9c, test `test_merged_performance_drawdown.py`). Verificado en Chrome: "All-time max 0.37 %".
- **"Max leverage 5x"** al lado de tarjetas con techo 3x: la fila nombra ahora los dos límites
  ("5x · trend book ≤3x"); `/api/risk.account.trend_leverage_cap`.
- **El PnL diario/semanal MTM se re-basaba en cada reinicio** (+0,29 $ → −0,003 $ a través del redeploy,
  con el libro igual): el baseline de PnL abierto se sembraba en la primera marca de cada proceso, y una
  pérdida abierta anterior al reinicio dejaba de contar contra el límite diario. Ahora viaja en
  `data/risk_peak.json` con clave de día e ISO-semana y se restaura si el periodo sigue vivo (5c6c34a,
  4 tests). **Verificado con un reinicio controlado dentro del CT** (`hostname: botstrike`):
  `day_start_unrealized −2.218484` idéntico antes y después, `error lines 0`; la identidad
  `daily_mtm = daily_real + (unreal − baseline)` se cumple en 5 muestras con un residuo de
  −0,018 … −0,002 $ (las marcas del risk loop van hasta 5 s por detrás).
- Pendiente de observar: ejecución del 2026-09-06 04:05 UTC (primera con caché Yahoo limpia; XAG de
  0,079 a ~0,040 de peso).

## Séptima ronda — régimen, contabilidad y "¿todo de Strike?" (bf3a875, 480 tests)
- **Régimen "siempre RANGING"**: el chip es el clasificador INTRADÍA (velas de 15 min de BTC/ETH/SOL/ADA:
  ADX14, momentum 20 barras, percentil de volatilidad, cruce de EMAs; RANGING / TRENDING_UP / _DOWN /
  BREAKOUT; permanencia mínima 30 min). Solo lo consumen las estrategias intradía (todas al 0 %) y las
  notificaciones; el libro trend diario no lo usa ("daily · n/a"). Medido en 30 días de velas asentadas
  da RANGING 56-60 %, TRENDING 30-35 %, BREAKOUT 9-10 % (~5 cambios/día por símbolo). El "siempre
  RANGING" del CT era un artefacto de reinicio: con ~15 h de velas el percentil 60 del ADX salía 61 (el
  ADX de Wilder arranca en 60+), y con ese umbral nada podía ser TRENDING; SOL quedaba UNKNOWN. Arreglo:
  los umbrales ignoran las 42 barras de calentamiento, caen a valores de manual con ventana corta y se
  acotan (ADX 20-30, momentum 0,5-3 %); la semilla del reinicio cubre 33 h paginando el venue.
  Verificado en el CT: 1.917-1.969 barras por símbolo, los cuatro clasificados en 6 s, umbrales ADX
  21,9-30,0, y RANGING es hoy correcto (BTC ADX 24 con vol en el percentil 1; ETH ADX 6).
- **Contabilidad re-conciliada por API**: Σ cash_effect = realizado (+9,6005), PnL abierto por posición =
  (mark − entrada) × tamaño en las 6, equity = inicial + realizado + abierto en /performance, /risk y
  /account (1.007,14), pico y drawdown iguales, exposición = Σ nocionales, funding Σ = perf.funding_paid
  (−0,0603), fees Σ = perf.total_fees (0,1409), win rate / avg win iguales, diario/semanal MTM continuos
  tras reinicio.
- **¿Usar la API de Strike para todo?** Lo que toca dinero ya es de Strike: marks (5 s), fills al mark,
  funding, reglas de orden, fees. Solo la HISTORIA de señal es Yahoo/Binance, y debe seguir siéndolo:
  las velas diarias de Strike empiezan en abril de 2026 (S&P: 16 ago, 20 velas) frente a 10 años de
  validación; el oro no cruza ni un trade en 42 de 135 días, la plata en 30; fines de semana con velas
  casi planas (mediana 0,00-0,65 % vs 0,8-1,9 % entre semana) que hundirían la vol objetivo; el S&P
  negocia 0,068 contratos/día de mediana. Base Strike−Yahoo en el solape (92 d): mediana ≈ 0 (oro
  +0,002 %, plata −0,09 %, WTI −0,30 %); si se usaran las velas de Strike, la señal Donchian-20 cambiaría
  en 4-12 % de los días, casi todo por la hora de cierre distinta. Añadido un **monitor de base**
  (mark de Strike / último cierre asentado) por mercado: log en cada ejecución y cada hora, aviso > 2,5 %,
  `/api/trend.basis`. Hoy: cripto ±0,2 %, S&P −0,15 %, WTI −0,16 %, oro −1,22 %, plata −1,07 %.

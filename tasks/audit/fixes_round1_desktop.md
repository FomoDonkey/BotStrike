# Fixes round 1 — Desktop (Bridge URL configurable + P0/P1/P2-lint)

Fecha: 2026-08-30 · Base: `tasks/audit/05_desktop.md` · Ámbito: `desktop/**` + `deploy/README.md` · Sin commit.

## Progreso (una línea por paso)
1. `src/lib/config.ts` (nuevo): URL/token en localStorage (`botstrike.bridgeUrl`, `botstrike.authToken`), default `http://127.0.0.1:9420`, `normalizeBridgeUrl`/`validateBridgeUrl` (host, host:port, http(s)://…; sin puerto → 9420; rechaza ftp://, credenciales, vacío), `getBridgeUrl/getBridgeWsUrl (http→ws, https→wss)/isLocalBridge/getBridgeMode/getBridgePort/getBridgeToken`, `setBridgeUrl/setBridgeToken` + `subscribeBridgeConfig` + hook `useBridgeConfig()` (useSyncExternalStore). Verificado en node (transpileModule): 11 casos de normalización + notificaciones OK.
2. Hardcodes eliminados: `constants.ts` (BRIDGE_URL/BRIDGE_WS_URL → bloque de constantes de timing + `WS_CHANNEL_LIST`), `ws.ts` (URL resuelta en `connect()`), `BacktestPage.tsx` (usa `api.backtestRun`), `ConnectionOverlay.tsx` (muestra URL real + badge LOCAL/REMOTE, fase `unreachable` a 15 s con Retry/Settings, sin eslint-disable: "connected" es derivado), `SystemPage.tsx` (detalle = URL real + canales abiertos). grep `127.0.0.1:9420|localhost:9420` en `src/` → sólo `config.ts` (default).
3. `api.ts`: `ApiError{status,isAuth}`, HTTP≠2xx → throw (mensaje del body `error`/`detail`), 200+`{error}` legacy → throw, timeout → ApiError, TypeError (red/CORS/CSP) → "Cannot reach …"; `authed()` añade `?token=` en start/stop/backtest: token configurado > (si bridge local) descubierto en `/api/bot/status` (cache por URL, invalidado en 401); `probeBridge(url)`; interfaces de respuesta tipadas (Health, BotStatus, Config, Performance, Strategies, Trades, DataCatalog, Backtest) → 0 `any`.
4. `ws.ts`: `lastMessageAt` + ping 10 s + stale 25 s → `dropSocket()` + reconexión con backoff 3→30 s (jitter), `wantOpen` (un close tras `disconnect()` no reconecta), `_reconnectAttempts` reseteado, timers de escalonado rastreados y limpiados en `disconnectAll()`, `onChannelStatus()`; `systemStore`: `openChannels`, `onChannelStatus` (0 sockets → `bridgeConnected=false` inmediato), `resetConnection()`, `onHealth` marca `bridgeConnected=true`, mensajes tipados; `useWebSocket.ts`: `startWebSockets()` llama `ensureLocalEngine()` (no-op si remoto), `restartWebSockets()`, handlers tipados con `BridgeMessage`.
5. Settings → pestaña **Connection** (primera tab; `navigate("/settings",{state:{tab:"connection"}})` desde el overlay): Bridge URL con validación en vivo y preview del origin normalizado, Auth token (password + ojo), **Test connection** (GET /api/health contra la URL del input, 4 s; muestra `status · engine running/stopped · mode · vX · N ms` o el error), **Save & reconnect** (persiste + `restartWebSockets()`), panel Active URL / Bridge / Engine / WS channels n/5. TopBar: badge LOCAL/REMOTE (`title`=URL), punto = bridge alcanzable, icono verde=feed / ámbar=bridge sin feed / rojo=sin bridge; badge HL/BIN ahora suscrito al store.
6. Rust `lib.rs`: sin lanzamiento incondicional en `setup`; `#[tauri::command] async ensure_local_engine(port)` (probe 127.0.0.1:port 300 ms, idempotente, guarda `Child` en `EngineProc` via `app.manage`), `--port` sólo si ≠ 9420 (el exe empaquetado es de 2026-04-05; no se asume `--host`), `kill_engine` en `RunEvent::Exit` y antes de `app.restart()` tras update; error del updater logueado; progreso de descarga sin spam. `tauri_plugin_shell` eliminado (`Cargo.toml`, `lib.rs`); `Cargo.lock` actualizado por cargo (botstrike 2.12.0, −121 líneas de deps del shell). `cargo check` → exit 0.
7. `tauri.conf.json`: CSP explícita (`default-src 'self'`, `script-src 'self'`, `style-src 'self' 'unsafe-inline' fonts.googleapis.com`, `font-src 'self' data: fonts.gstatic.com`, `img-src 'self' data: blob:`, `connect-src 'self' ipc: http://ipc.localhost http://127.0.0.1:9420 ws://127.0.0.1:9420 http://localhost:9420 ws://localhost:9420 http://*:9420 ws://*:9420 https://*:9420 wss://*:9420`, `object-src 'none'`, `base-uri 'self'`, `form-action 'none'`) + `devCsp` (unsafe-eval/inline + localhost:1420 para HMR). Google Fonts se mantienen (self-host requiere descargar woff2 → fuera de "trivial"). `capabilities/default.json` → sólo `core:default`; `@tauri-apps/plugin-shell` desinstalado (package.json + package-lock). **CSP no verificada en runtime** (requiere `tauri dev`).
8. `eslint.config.js`: `globalIgnores(['dist','src-tauri/**','node_modules'])` + `no-unused-vars` con `^_`; corregidos los 45 errores reales (any→tipos en api/ws/stores/CandlestickChart (tipos de lightweight-charts)/BacktestPage/PerformancePage/SystemPage, imports muertos en Dashboard/Performance/System/Trading, `no-empty` en tradingStore/CandlestickChart, `set-state-in-effect` en TopBar.PriceTicker y AnimatedNumber (estado derivado en render / setState sólo en callbacks), `preserve-manual-memoization` en PerformancePage). `npm run lint` → exit 0, 0 problemas.
9. Versión **2.12.0** en `package.json`, `tauri.conf.json`, `Cargo.toml` (+ `Cargo.lock`); `vite.config.ts` `define.__APP_VERSION__` desde package.json, `src/vite-env.d.ts` declara el global; `SystemPage` lo muestra (verificado en `dist/`: literal `2.12.0` presente, `__APP_VERSION__` no queda).
10. `deploy/README.md` → sección "Acceso desde el desktop (≥ 2.12.0)": Settings → Connection, formato de URL, cómo obtener el token (`pct exec 104 -- grep AUTH_TOKEN /opt/botstrike/app/.env`), Test / Save & reconnect, troubleshooting.

## Verificación (salida real, 2026-08-30, `desktop/`)
- `npm run build` → `tsc -b && vite build` … `✓ built in 33.95s` — **exit 0** (chunk principal 857 kB, mismo warning de tamaño que antes).
- `npm run lint` → `eslint .` sin salida — **exit 0** (antes: 64 errores).
- `cargo check --manifest-path src-tauri/Cargo.toml` → `Compiling botstrike v2.12.0 … Finished dev profile in 31.05s` — **exit 0**.
- No ejecutado: `tauri build`/`tauri dev` (fuera de alcance) → CSP, `ensure_local_engine` y el flujo Settings→Test→Save quedan **no verificados en runtime**.

## Pendiente / fuera de alcance
- `tasks/todo.md`, `tasks/lessons.md`, `README.md` raíz (badge 1.5.0 / "localhost:9420") y `desktop/README.md` (plantilla Vite): no tocados por la restricción de alcance.
- `@tanstack/react-query` sigue como dependencia muerta (P3, no pedido).
- P3 restantes de la auditoría (fuentes self-host, code-splitting, selectores en SystemPage/DataPage, `.gitignore` de `desktop/data`).

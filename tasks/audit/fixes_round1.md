# Fixes Round 1 — P0/P1 de auditorías 01 (F01, F02, F03+F09) y 02 (P0-01, P0-02, P0-03, P1-04, P1-05)

**Fecha:** 2026-08-30 · **Registro incremental** (una línea por fix, con archivo:línea y verificación). Sin commit.

| # | Fix | Estado | Archivos | Verificación |
|---|-----|--------|----------|--------------|
| 1 | **F01 = P0-03** Posiciones desnudas: `close_all_positions()` ANTES de `cancel_all()` en shutdown y en halt por DD (una sola vez) | HECHO | `exchange/binance_client.py:729` (`close_all_positions`: positionRisk -> MARKET reduceOnly por posicion, qty floor a stepSize, 3 intentos con relectura) - `execution/order_engine.py:544` (`close_all_positions`: usa la nativa del cliente o fallback `get_positions` + MARKET reduceOnly con retry) - `execution/paper_simulator.py:344` (`close_all_positions(reason)` cierra a ultimo precio con slippage taker) - `main.py:839` (`_flatten_all`: cerrar -> luego `cancel_all`; paper procesa fills por el pipeline normal y llama `notify_external_exit`) - `main.py:772` (halt DD: flag `_dd_flattened`, flatten UNA vez, se resetea al recuperar) - `main.py:886-893` (shutdown: guard `_shutdown_flatten_done`; con `close_positions_on_shutdown=False` NO se cancela nada -> SL/TP del exchange siguen vivos) - `config/settings.py:93` (`close_positions_on_shutdown: bool = True` con comentario) | tests `test_shutdown_closes_positions_before_cancel_all`, `test_shutdown_keep_positions_policy_never_cancels_protectives`, `test_shutdown_paper_and_dry_run_never_touch_exchange`, `test_drawdown_halt_flattens_once_then_cancels` (loop real ~20 ciclos -> 1 close + 1 cancel), `test_engine_close_all_positions_fallback_uses_get_positions`, `test_binance_close_all_positions_rounds_and_reduce_only`, `test_paper_close_all_positions` |
| 2 | **F02** `exit_fibonacci` no reconocido como exit en live | HECHO | `execution/order_engine.py:79` (`is_exit_signal`: `action.startswith("exit")` o `trailing_stop_hit`/`mm_unwind` o `exit_reason`) usado en `:108`; mismo criterio que `paper_simulator._execute_one` y `risk_manager.validate_signal`; `main.py:557` lo reutiliza para filtrar exits | `test_is_exit_signal_startswith_exit`, `test_exit_fibonacci_routed_as_market_reduce_only` (MARKET + reduceOnly, sin SL/TP), `test_paper_and_live_share_exit_criterion` |
| 3 | **F03 + F09** `_performance_factor` mal escalado / desactivacion permanente / salidas saltadas por los gates | HECHO | `portfolio/portfolio_manager.py:73-79` (constantes: 20 trades cerrados minimos, ventana 50, floor 0.5, cooldown 3600 s) - `:222` (`_performance_factor`: solo trades cerrados, normalizado en R-multiplos = pnl medio / presupuesto de riesgo por trade, `tanh`, clamp [0.5,1.5], WARNING `strategy_allocation_reduced_by_performance`) - `:254` (`update_strategy_pnl(..., is_exit=None)`: entradas pnl=0 no cuentan) - `:312/:321` (`should_strategy_trade`: bloqueo con WARNING `strategy_disabled_by_performance`; tras el cooldown se re-habilita en probation con ventana limpia -> nunca permanente) - `main.py:534-557` (`entries_allowed` = should_activate AND should_strategy_trade; con posicion abierta SIEMPRE se llama `generate_signals` y solo pasan exits; sin posicion y bloqueado -> skip; `get_allocation` solo si hay entradas permitidas) | `test_performance_factor_requires_min_closed_trades`, `test_performance_factor_floor_and_warning` (capture_logs), `test_performance_block_is_not_permanent` (reloj inyectado `_now`), `test_exit_executed_when_regime_gate_blocks_entries`, `test_exit_executed_when_performance_gate_blocks_entries`, `test_no_entry_when_gates_block_and_no_position`, `test_entry_signal_generated_when_gates_allow` |
| 4 | **P0-01** Precision de ordenes (stepSize/tickSize/minQty/minNotional) | HECHO | `exchange/binance_client.py:59-77` (`DEFAULT_SYMBOL_FILTERS` fallback real + `GENERIC_SYMBOL_FILTER`) - `:79-120` (`floor_to_step`, `round_to_tick`, `format_decimal` sin notacion cientifica, `parse_symbol_filters`) - `:392` (`load_exchange_info` una vez, cache, reintento max. cada 300 s, fallback seguro) - `:455` (`_normalize_order_params`: qty floor a step, price/stopPrice a tick con redondeo conservador SELL->floor / BUY->ceil en triggers, minQty y MIN_NOTIONAL locales salvo reduceOnly) - aplicado en `place_order` (`:670`), `batch_orders` (`:909`), `close_all_positions` (`:729`) y por tanto en entrada, SL/TP, emergencia y close_all del engine | `test_rounding_helpers`, `test_parse_exchange_info_filters`, `test_order_params_respect_filters` (BTC 0.007692->0.007, 65432.15->65432.1; SOL 1.666667->1.66; ADA 428.571429->428 y stop 0.34871->0.3487), `test_min_qty_and_min_notional_rejected_locally`, `test_filters_fallback_when_exchange_info_fails`, `test_place_order_sends_rounded_params_and_result_resp_type` |
| 5 | **P0-02** Retries no idempotentes de `POST /fapi/v1/order` | HECHO | `exchange/binance_client.py:243` (`_IDEMPOTENT_POST_PATHS`: solo leverage/marginType/listenKey/positionSide se reintentan a ciegas) - `:248` (`_retry_request(idempotent, recover_fn)`: POST no idempotente NUNCA se reenvia tras timeout/5xx sin confirmar) - `:300` (`_recover_or_raise`: consulta por `origClientOrderId`; si existe -> devuelve ese resultado; si -2013 -> un reenvio con el MISMO cid; si no se puede saber -> raise) - `:626` (`new_client_order_id`: prefijo + ms + uuid, <=36) - `:643` (`get_order` GET /fapi/v1/order, None en -2013) - `:670-726` (`place_order`: cid SIEMPRE, `recover_fn=_recover`) - `batchOrders` sin reenvio ciego; DELETE mantiene retries (cancelar dos veces no duplica nada) | `test_retry_request_non_idempotent_raises_without_recover` (fn llamada 1 vez), `test_place_order_timeout_recovers_by_client_order_id` (1 POST + GET origClientOrderId), `test_place_order_5xx_resends_only_if_order_does_not_exist` (2 POST mismo cid), `test_place_order_5xx_with_unknown_state_raises`, `test_batch_orders_not_resent_on_timeout` - sesion aiohttp falsa, sin red |
| 6 | **P1-04** Order book WS `b`/`a` | HECHO | `exchange/binance_ws.py:150-153` (`data.get("b", data.get("bids"))`, idem `a`; propaga `E`/`T` del payload) | `test_ws_depth_parser_reads_b_and_a` (payload real del stream + compat `bids/asks`) |
| 7 | **P1-05** SL/TP solo tras FILLED; -2022 con retry | HECHO | `exchange/binance_client.py:716` (`newOrderRespType=RESULT` en MARKET) - `execution/order_engine.py:218` (protectivas solo si `_await_fill` > 0 y no es exit) - `:235` (`_await_fill`: FILLED en el ACK -> executedQty; terminal sin fill -> 0 y sin protectivas; NEW/PARTIALLY_FILLED -> poll `get_order` por cid x5) - `:294-318` (`_place_with_retry`: 3 intentos con backoff 0.1/0.3/0.9 s en -2022) - `_place_protective_orders` dimensiona SL/TP con la qty EJECUTADA; emergencia solo si ambas agotan los reintentos | `test_protectives_placed_immediately_when_ack_is_filled`, `test_protectives_wait_for_fill_when_ack_is_new` (3 polls, protectivas con executedQty 0.1 tras el ultimo poll), `test_no_protectives_when_entry_expired_unfilled`, `test_reduce_only_rejected_is_retried_before_emergency`, `test_emergency_close_after_all_protective_retries_fail` |

## Verificacion final
- `py -3.12 -m pytest tests/ -q -p no:cacheprovider` -> **70 passed** (36 previos + 34 nuevos en `tests/test_p0_round2.py`).
- `py -3.12 -c "import main, server.bridge"` -> OK.
- Archivos tocados: `main.py`, `execution/order_engine.py`, `execution/paper_simulator.py`, `portfolio/portfolio_manager.py`, `exchange/binance_client.py`, `exchange/binance_ws.py`, `config/settings.py`, `tests/test_p0_round2.py`, este registro. Sin commit.

## Notas / decisiones
- `DEFAULT_SYMBOL_FILTERS["BTCUSDT"].minNotional = 100` (la tabla de la auditoria dice 50 y el texto F13 dice 100; se toma el valor conservador; es solo fallback, exchangeInfo real lo sobrescribe).
- Shutdown con `close_positions_on_shutdown=False`: se mantienen posiciones Y se omite `cancel_all` (nunca cancelar sin cerrar). El halt por DD siempre aplana.
- Paper: el flatten cierra las posiciones del simulador y pasa los fills por `_process_paper_fill` (PnL realizado registrado) + `notify_external_exit` para limpiar estado/cooldown de la estrategia.
- Pendiente (fuera de alcance de esta ronda): re-proteccion de posiciones existentes al arrancar (P0-03.3), OCO explicito (P1-12), equity real al arrancar (F07/P1-10).

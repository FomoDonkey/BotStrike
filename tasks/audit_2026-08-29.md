# BotStrike — Auditoría total, investigación SOTA y despliegue (2026-08-29/30)

Informe consolidado. Detalle por dominio en `tasks/audit/01..05_*.md`, investigación en `tasks/research_sota_2026.md`, registro de fixes en `tasks/audit/fixes_round1*.md`. Todo lo que aquí se afirma como "verificado" se ejecutó de verdad (tests, curl, backtests) y la salida está en los archivos citados.

## 1. Resumen ejecutivo (5 puntos)

1. **Desplegado y verificado**: v2.12.1 corre en Proxmox CT 104 (`192.168.1.204:9420`) como servicio systemd con engine **paper** autoarrancado, health real, watchdog y firewall. `deploy/verify.sh` → PASS; suite 92/92 en local y **dentro del CT**.
2. **Auditoría: 119 hallazgos** (01 core 28 · 02 exchange 23 · 03 bridge 27 · 04 backtests 19 · 05 desktop 22). **Todos los P0 (12) corregidos en esta sesión** y los P1 de mayor impacto; el resto queda priorizado en §5.
3. **Evidencia cuantitativa (nueva, datos correctos)**: sobre futuros USDT-M 1m, 150 días (2026-04-01→08-29), 4 símbolos: MR PF 0.40–0.61 y FIB PF 0.61–1.01; **7 de 8 combinaciones pierden**; pool 2.925 trades, PnL −$1.448 sobre $1.000; **edge negativo ANTES de fees** (−4.0 bps/trade MR, −1.8 FIB); t = −13.2 (MR); MR pierde en 20/20 bloques de 30 días. No es varianza ni fees: es ausencia de edge.
4. **Bloqueo regulatorio verificado**: Binance cesó servicios de trading para residentes en España el 1-jul-2026 (MiCA). El venue principal no existe para el dueño; Hyperliquid (único donde el diseño es ejecutable) está integrado a medias y con bugs que impiden activarlo.
5. **Veredicto**: **NO pasar a live** con MR/FIB en ningún venue. El valor real construido es la infraestructura (pipeline paper≈live, riesgo, ejecución, observabilidad, despliegue), no las estrategias. El siguiente trabajo útil es buscar edge con hipótesis nuevas sobre un backtester fiel al live, no re-tunear RSI/ATR.

## 2. Estado del arte 2026 vs BotStrike (síntesis de `research_sota_2026.md`)

| Tema | SOTA 2026 | BotStrike hoy |
|---|---|---|
| Fees Binance USDT-M VIP0 | maker 2 / taker 5 bps → 7–10 bps round-trip; σ BTC 1 min ≈ 6 bps | config 4 bps taker; estrategias con 14 bps hard-coded |
| Estrategias con edge neto documentado | trend/TS-momentum a 1–4 semanas (Sharpe > 1.5); funding-arb pequeño; stat-arb marginal | MR 1–5 min y Fibonacci (sin evidencia; aquí medidas negativas); trend **archivado** |
| ML/LLM agents | fallan en replicación (FinMem −72 % post-cutoff; RL < buy&hold con costes) | no usa ML — correcto |
| Validación | CPCV/walk-forward purgado, DSR > 0.95, PBO < 0.20, ≥ 300 trades OOS, stress fees ×1.5 | split único 7d/3d, selección sobre test, sin DSR/PBO (ver 04) |
| Riesgo | 0.5–1 % por trade, ¼ Kelly, stop en exchange, kill switch externo | Kelly/vol-target decorativos (nunca modifican tamaño); stop en exchange sí |
| Infra 24/7 | systemd + watchdog + sdnotify, chrony, backup SQLite, alertas "posición sin stop" | systemd + watchdog crash-only (nuevo), Telegram; falta backup DB y alerta de posición sin stop |
| Regulación | MiCA (jul-2026); ESMA: perps = CFD, apalancamiento retail 2:1 en venues UE | diseñado para Binance 5x |

## 3. Evidencia de backtests (04 §2, ejecutados hoy)

| Símbolo | Estrat. | Trades | WR | PF | PnL neto $ | PnL fees×2 $ | Max DD | t-stat |
|---|---|---|---|---|---|---|---|---|
| BTC | MR | 503 | 39.6 % | 0.43 | −212.7 | −391.8 | 21.6 % | −7.7 |
| ETH | MR | 531 | 39.0 % | 0.40 | −300.2 | −479.0 | 30.3 % | −9.0 |
| SOL | MR | 424 | 43.6 % | 0.46 | −255.5 | −400.0 | 25.9 % | −7.0 |
| ADA | MR | 383 | 53.5 % | 0.61 | −190.4 | −322.0 | 20.8 % | −4.2 |
| BTC | FIB | 247 | 17.0 % | 0.81 | −74.4 | −172.0 | 12.7 % | −1.2 |
| ETH | FIB | 280 | 17.1 % | 0.61 | −212.1 | −314.3 | 24.8 % | −3.3 |
| SOL | FIB | 273 | 15.0 % | 0.66 | −208.5 | −309.7 | 24.8 % | −2.4 |
| ADA | FIB | 284 | 19.4 % | 1.01 | +6.2 | −111.3 | 12.0 % | +0.05 |

Pool MR (1.841 trades): PnL bruto antes de fees −$325 → edge bruto −4.0 bps/trade; bootstrap 95 % [−1.099, −813]; PSR(0)=0.000. Pool FIB (1.084): bruto −$70 → −1.8 bps; PSR=0.005. Con fees 5 bps + slippage en SL + funding real: todos negativos (ADA FIB −$48).

Caveats del propio informe: el motor simple alimenta 501 barras (filtro 1H de MR con 8 velas) — la variante live (2000 barras) cambia el signo del filtro, lo que refuerza el hallazgo de que **nadie ha validado lo que corre en producción**; FIB en backtest no ejecuta `exit_fibonacci` (P0 corregido en live, pendiente en backtester).

## 4. Corregido en esta sesión (3 commits: c18bb32, b3dbf75, ffacf4a)

**Bridge/deploy (P0/P1)** — `BOTSTRIKE_AUTOSTART` no lo leía nadie (bridge 5,3 h "active" con engine parado) → autostart en lifespan; `auth_token` regalado en `/api/bot/status` → token por `.env`, oculto y exigido (401) en bind público, `/docs` off; health real (engine/WS/tick age → 503) + watchdog crash-only (backoff, `os._exit(3)` → systemd); backtest en hilo (bloqueaba el event loop) + validación de símbolo; crash del engine logueado; `pyarrow` explícito, `requirements.lock` del CT, `requirements-dev.txt`; scripts `verify.sh`/`host_deploy.sh`/`remote_deploy.sh`.

**Core/exchange (P0/P1)** — `close_all_positions()` ANTES de `cancel_all()` en shutdown y halt por DD (antes: SL/TP cancelados con la posición abierta a 5x); `is_exit_signal()` (`exit_fibonacci` iba como entrada en live); performance factor no permanente + salidas siempre gestionadas; `exchangeInfo` (stepSize/tickSize/minNotional: órdenes BTC/SOL/ADA eran rechazadas); retries no idempotentes de `POST /order` (posiciones duplicadas) → recuperación por `origClientOrderId`; depth WS `b`/`a` (order book siempre vacío); SL/TP solo tras FILLED con `executedQty`.

**Desktop (P0/P1)** — URL del bridge y token configurables (Settings → Connection, Test, Save & reconnect), badge LOCAL/REMOTE, errores HTTP/`{error}` visibles (start live fallaba en silencio), WS con detección de socket medio-abierto, engine local solo si bridge local y matado al salir, CSP real, capabilities mínimas, lint 64→0, versiones sincronizadas 2.12.0. Verificado: `npm run build` ✓, `npm run lint` ✓, `cargo check` ✓ (runtime `tauri dev` NO probado).

## 5. Pendiente, priorizado

**P0 (antes de cualquier decisión de live)**
1. Venue legal: Binance no es opción para residentes en España. Hyperliquid: arreglar SL/TP en el SDK (`float_to_wire`), testnet real, y auditar de nuevo `hyperliquid_client.py` (02 P1-13). Alternativa: no operar live.
2. Backtester fiel al live (04 §lista 1–3): `exit_*` genérico en `backtester.py`, ventana `MAX_BARS`=2000 compartida con `market_data.py`, un solo motor con `RiskManager`+`PortfolioManager` y FIB completo.
3. Congelar MR/FIB hasta que un backtest con costes reales muestre edge bruto > 0, PSR > 0.95, ≥ 300 trades OOS en ≥ 2 símbolos (hoy: negativo).

**P1**
4. 01 F04–F08: `bars_held` por timestamp (no `len`), filtro 1H con klines 1H reales y ADX calentado, puerta de R:R neto ≥ 1.5 y ATR ≥ 2× coste, equity/peak desde el exchange + persistencia, notificar a la estrategia los SL/TP ejecutados en exchange y reconstruir estado tras reinicio.
5. 02: re-proteger posiciones existentes al arrancar; OCO manual (cancelar TP al saltar SL y viceversa); user stream con URL de testnet; listenKey keepalive verificado.
6. 03: persistencia del estado paper (equity/posiciones sobreviven reinicios), backup de `trade_database.db` (Litestream o cron), alerta Telegram "posición sin stop", `sdnotify`+`WatchdogSec`.
7. 04 §4–7: fee 5 bps, slippage en SL/TP y fill al open siguiente, funding real por `fundingTime`, métricas con timestamps normalizados, walk-forward anclado con embargo ≥ 24 h + DSR/PBO.
8. Desarchivar trend/TS-momentum a horizonte semanal y evaluarlo con el backtester de (2) — única familia con evidencia neta de costes.

**P2/P3**: ver tablas de cada informe (velas alineadas a reloj, `compute_all` fuera del callback WS, VPIN bucket por activo, chunk 843 kB del desktop, deps muertas, README raíz desactualizado, `scripts/` rotos).

## 6. Operación

- Deploy: `git push origin main && bash deploy/remote_deploy.sh` (Tailscale) — hace update → install (idempotente) → `verify.sh`.
- Estado: `ssh root@100.68.139.93 pct exec 104 -- bash /opt/botstrike/app/deploy/verify.sh`; logs `journalctl -u botstrike-bridge -f`.
- Desktop ≥ 2.12.0: Settings → Connection → `http://192.168.1.204:9420` + token (`pct exec 104 -- grep AUTH_TOKEN /opt/botstrike/app/.env`). **Requiere compilar/instalar la nueva versión del desktop** (`npm run tauri:build`), no hecho en esta sesión.
- Red: ProtonVPN con kill switch bloquea la LAN y el login de Tailscale; desactivarla (o "Allow LAN") antes de desplegar.
- El paper que corre ahora en el CT es un **soak test de infraestructura**, no una validación de estrategia: la evidencia ya dice que MR/FIB no tienen edge.
